# Clone the repository, switch into it
git clone https://gitlab.com/timvisee/cant-touch-this.git
cd cant-touch-this

# Move to the script directory
cd ci

# Invoke the script as sudo
sudo ./install_sdk
